error: `def` is a statement and cannot be used inside a pipeline
  --> reject/pipelines/def-in-pipeline.nu:1:1
  |
1 | def f [] { } | length
  | ^^^
  = help: declarations and the module, source, overlay and plugin commands are statements of their own
